void main(List<String> args) {
  // Vòng lặp trong dart - DART #10 - Beginner | Lập trình Dart
  // Vòng lặp for
  // Vòng lặp while
  // Vòng lặp do while
  // Vòng lặp for in
  // Vòng lặp for each
  // Vòng lặp lồng nhau
  // Vòng lặp vô hạn
  // Vòng lặp với break và continue
  // Vòng lặp với label
  // Vòng lặp với forEach
  // Vòng lặp với map
  // Vòng lặp với where
  // Vòng lặp với every
  // Vòng lặp với any
  // Vòng lặp với reduce
  // Vòng lặp với fold
  // Vòng lặp với skip
  // Vòng lặp với take
  // Vòng lặp với skipWhile
  // Vòng lặp với takeWhile
  // Vòng lặp với expand
  // Vòng lặp với sort
  // Vòng lặp với sublist
  // Vòng lặp với shuffle
  // Vòng lặp với asMap
  // Vòng lặp với cast
  // Vòng lặp với toSet
  // Vòng lặp với toList
  // Vòng lặp với toMap
  // Vòng lặp với toSet
  // Vòng lặp với toUnmodifiable
  // Vòng lặp với toUnmodifiableView
  // Vòng lặp với toUnmodifiableListView
  // Vòng lặp với toUnmodifiableSet
  // Vòng lặp với toUnmodifiableMap
  // Vòng lặp với toUnmodifiableList
  // Vòng lặp với toUnmodifiableMap

// Vòng lặp for
  // for (var i = 0; i < 10; i++) {
  //   print('i = $i');
  // }
  // ----------------------------------------

  // - Vòng lặp vô hạn
  // int i = 0;
  // for (;;) {
  //   print('i = $i');
  //   i++;
  // }
  // ----------------------------------------

  // duỵet qua từng phần tử trong List

  // List cars = ['BMW', 'Audi', 'Toyota', 'Honda', 'KIA'];
  // for (var i = 0; i < cars.length; i++) {
  //   print(cars[i]);
  // }

  // - For in

  // for (var car in cars) {
  //   print(car);
  // }
// ----------------------------------------

  // - While
  // int a = 10;
  // while (a < 10) {
  //   print('a = $a');
  //   a++;
  // }

  // - Do while

  // int i = 10;
  // do {
  //   print('i = $i');
  //   i++;
  // } while (i < 10);
// ----------------------------------------

  // - break và continue
  // for (var i = 0; i < 10; i++) {
  //   if (i == 5) {
  //     break;
  //   }
  //   print('i = $i');
  // }
  // print('Kết thúc vòng lặp');
  // for (var i = 0; i < 10; i++) {
  //   if (i == 5) {
  //     continue;
  //   }
  //   print('i = $i');
  // }
  // print('Kết thúc vòng lặp');

// ----------------------------------------
  // - For each
  // cars.forEach((element) {
  //   print(element);
  // });
}
